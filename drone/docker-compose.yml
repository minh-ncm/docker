services:
  server:
    restart: unless-stopped
    image: drone/drone:2.28
    ports:
      - "80:80"
      - "443:443"
    env_file:
      - .env
  runner:
    restart: unless-stopped
    image: drone/drone-runner-docker:1.8
    depends_on:
      - server
    env_file:
      - .env